ideas = {
	
	head_of_government = {	# 정부 수반(총리, 왕..등등) - 내정 보너스 관련	
		generic_communist_revolutionary = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = ROM tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = communist_revolutionary limit = 1 }
				}
				
			}

			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_the_lee_affair
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}
			
			traits = { communist_revolutionary }
	
			on_add = {
				#country_event = political.1
			}
	
			do_effect = {
				NOT = {
					has_government = communism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
			
		generic_democratic_reformer = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = democratic_reformer limit = 1 }
				}
				
			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					OR = {
						#has_completed_focus = NZL_strengthen_the_commonwealth
						#has_completed_focus = NZL_constitution_amendment_act
					}
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}

			
			traits = { democratic_reformer }
	
			on_add = {
				#country_event = political.13
			}
	
			do_effect = {
				NOT = {
            OR = {
                has_government = socialism
                has_government = social_democracy
                has_government = social_liberalism
                has_government = market_liberalism
                has_government = social_conservatism
            }
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
		
		generic_fascist_demagogue = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = ROM tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = fascist_demagogue limit = 1 }
				}

			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_in_the_darkness
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}				
			}
			
			traits = { fascist_demagogue }
	
			on_add = {
				#country_event = political.7
			}
	
			do_effect = {
				NOT = {
					has_government = fascism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
	}
	
	foreign_minister = {	# 외교통상부 장관 - 외교 보너스 및 동맹 관련
		generic_communist_revolutionary = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = ROM tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = communist_revolutionary limit = 1 }
				}
				
			}

			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_the_lee_affair
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}
			
			traits = { communist_revolutionary }
	
			on_add = {
				#country_event = political.1
			}
	
			do_effect = {
				NOT = {
					has_government = communism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
			
		generic_democratic_reformer = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = democratic_reformer limit = 1 }
				}
				
			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					OR = {
						#has_completed_focus = NZL_strengthen_the_commonwealth
						#has_completed_focus = NZL_constitution_amendment_act
					}
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}

			
			traits = { democratic_reformer }
	
			on_add = {
				#country_event = political.13
			}
	
			do_effect = {
				NOT = {
            OR = {
                has_government = socialism
                has_government = social_democracy
                has_government = social_liberalism
                has_government = market_liberalism
                has_government = social_conservatism
            }
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
		
		generic_fascist_demagogue = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = ROM tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = fascist_demagogue limit = 1 }
				}

			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_in_the_darkness
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}				
			}
			
			traits = { fascist_demagogue }
	
			on_add = {
				#country_event = political.7
			}
	
			do_effect = {
				NOT = {
					has_government = fascism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
	}
	
	economy_minister = {	# 산업통상부 장관 - 산업, 공학, 소비재 관련

		generic_captain_of_industry = {

			allowed = {
				OR = { original_tag = KOR original_tag = SER original_tag = ICE original_tag = SYR original_tag = LEB original_tag = EGY original_tag = JOR original_tag = CRO original_tag = LBA original_tag = INS original_tag = MAL original_tag = AZR original_tag = UKR original_tag = GEO original_tag = ARM original_tag = VIN original_tag = CAM original_tag = MNT original_tag = LIB original_tag = LAO original_tag = PAL }
			}
			
			traits = { captain_of_industry }
		}
		
		generic_fortification_engineer = {

			allowed = {
				OR = { original_tag = KOR original_tag = SER original_tag = ICE original_tag = SYR original_tag = LEB original_tag = EGY original_tag = JOR original_tag = CRO original_tag = LBA original_tag = INS original_tag = MAL original_tag = AZR original_tag = UKR original_tag = GEO original_tag = ARM original_tag = VIN original_tag = CAM original_tag = MNT original_tag = LIB original_tag = LAO original_tag = PAL }
			}
			
			traits = { fortification_engineer }
		}

		generic_war_industrialist = {

			allowed = {
				OR = { original_tag = KOR original_tag = SER original_tag = ICE original_tag = SYR original_tag = LEB original_tag = EGY original_tag = JOR original_tag = CRO original_tag = LBA original_tag = INS original_tag = MAL original_tag = AZR original_tag = UKR original_tag = GEO original_tag = ARM original_tag = VIN original_tag = CAM original_tag = MNT original_tag = LIB original_tag = LAO original_tag = PAL }
			}
			
			traits = { war_industrialist }
		}
	}
	
	security_minister = {	# 치안장관 - 국내 불만도 및 점령지 효율 등등
		generic_communist_revolutionary = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = ROM tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = communist_revolutionary limit = 1 }
				}
				
			}

			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_the_lee_affair
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}
			
			traits = { communist_revolutionary }
	
			on_add = {
				#country_event = political.1
			}
	
			do_effect = {
				NOT = {
					has_government = communism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
			
		generic_democratic_reformer = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = democratic_reformer limit = 1 }
				}
				
			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					OR = {
						#has_completed_focus = NZL_strengthen_the_commonwealth
						#has_completed_focus = NZL_constitution_amendment_act
					}
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}

			
			traits = { democratic_reformer }
	
			on_add = {
				#country_event = political.13
			}
	
			do_effect = {
				NOT = {
            OR = {
                has_government = socialism
                has_government = social_democracy
                has_government = social_liberalism
                has_government = market_liberalism
                has_government = social_conservatism
            }
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
		
		generic_fascist_demagogue = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = ROM tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = fascist_demagogue limit = 1 }
				}

			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_in_the_darkness
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}				
			}
			
			traits = { fascist_demagogue }
	
			on_add = {
				#country_event = political.7
			}
	
			do_effect = {
				NOT = {
					has_government = fascism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
	}
	
	intel_minister = {	# 첩보국장 - 곧 나올 dlc 첩보 효과 부분
		generic_communist_revolutionary = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = ROM tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = communist_revolutionary limit = 1 }
				}
				
			}

			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_the_lee_affair
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}
			
			traits = { communist_revolutionary }
	
			on_add = {
				#country_event = political.1
			}
	
			do_effect = {
				NOT = {
					has_government = communism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
			
		generic_democratic_reformer = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = democratic_reformer limit = 1 }
				}
				
			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					OR = {
						#has_completed_focus = NZL_strengthen_the_commonwealth
						#has_completed_focus = NZL_constitution_amendment_act
					}
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}

			
			traits = { democratic_reformer }
	
			on_add = {
				#country_event = political.13
			}
	
			do_effect = {
				NOT = {
            OR = {
                has_government = socialism
                has_government = social_democracy
                has_government = social_liberalism
                has_government = market_liberalism
                has_government = social_conservatism
            }
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
		
		generic_fascist_demagogue = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = ROM tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = fascist_demagogue limit = 1 }
				}

			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_in_the_darkness
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}				
			}
			
			traits = { fascist_demagogue }
	
			on_add = {
				#country_event = political.7
			}
	
			do_effect = {
				NOT = {
					has_government = fascism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
	}
	
	defence_minister = {	# 국방장관 - 제한적인 교리적 버프
		generic_communist_revolutionary = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = ROM tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = communist_revolutionary limit = 1 }
				}
				
			}

			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_the_lee_affair
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}
			
			traits = { communist_revolutionary }
	
			on_add = {
				#country_event = political.1
			}
	
			do_effect = {
				NOT = {
					has_government = communism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
			
		generic_democratic_reformer = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = democratic_reformer limit = 1 }
				}
				
			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					OR = {
						#has_completed_focus = NZL_strengthen_the_commonwealth
						#has_completed_focus = NZL_constitution_amendment_act
					}
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}
			}

			
			traits = { democratic_reformer }
	
			on_add = {
				#country_event = political.13
			}
	
			do_effect = {
				NOT = {
            OR = {
                has_government = socialism
                has_government = social_democracy
                has_government = social_liberalism
                has_government = market_liberalism
                has_government = social_conservatism
            }
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
		
		generic_fascist_demagogue = {
			
			allowed = {
				NOT = { OR = { tag = GER tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = AST tag = RAJ tag = CAN tag = SAF tag = ROM tag = CZE tag = PRC tag = CHI tag = MAN tag = MEX tag = HOL } }
				NOT = {
					has_available_idea_with_traits = { idea = fascist_demagogue limit = 1 }
				}

			}
			
			available = {
				if = {
					limit = { 
						original_tag = NZL
						has_dlc = "Together for Victory"
					}
					#has_completed_focus = NZL_in_the_darkness
				}
				if = {
					limit = { has_dlc = "Man the Guns" }	
					NOT = { has_autonomy_state = autonomy_supervised_state }
				}				
			}
			
			traits = { fascist_demagogue }
	
			on_add = {
				#country_event = political.7
			}
	
			do_effect = {
				NOT = {
					has_government = fascism
				}
			}
	
			ai_will_do = {
				factor = 0
			}
		}
	}
	
}
